<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <!-- Shared pattern: traceId (from OTel) + userId (from UserContextFilter/MdcUserIdFilter) -->
  <include resource="logback-common.xml"/>
</configuration>
